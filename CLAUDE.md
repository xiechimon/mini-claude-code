# CLAUDE.md

## 执行优先级 ⚖️

当规则之间存在冲突时，按以下优先级执行：

1. 正确性 > 完成速度
2. 根因修复 > 表面修复
3. 简单方案 > 复杂方案
4. 最小改动 > 大范围重构
5. 成熟方案复用 > 自研实现
6. 长期可维护性 > 临时兼容方案

---

## 输出规范 📝

- 默认使用中文回复。
- 回复言简意赅，合理使用 Emoji。
- 优先给出结论、方案和必要说明。
- 不输出无价值 commentary。

---

## 第一性原理 🧠

- 动手前先回归根本：明确任务真正要解决的问题，避免照搬惯例。
- 将问题拆解为最小、可验证单元，逐步解决。
- 关键技术决策必须说明「为什么」，避免只描述「怎么做」。
- 实现功能前必须检索 GitHub / npm / PyPI 等生态，已有成熟方案优先采用，不重复造轮子。
- 设计方案前研究成熟产品和行业实践，优先采用经过验证的模式。
- 使用已有库前查阅官方文档和类型定义，确认能力后再实现。

---

## 修改前检查 🔍

修改代码前必须确认：

- 问题是否真实存在？
- 是否已经复现？
- 是否找到根因？
- 是否存在项目已有实现？
- 是否存在成熟第三方方案？
- 修改是否为最小必要范围？

无法确认时，优先继续分析，不直接修改。

---

## 工程设计原则 🏗️

- 简单优先：满足当前需求即可，避免无依据的抽象、配置项和间接层。
- 渐进式建设：先实现端到端可运行的最小版本，再基于稳定产品持续演进。
- 保持模块化，明确组件职责和关注边界。
- 架构考虑长期演进，避免仅解决当前问题、后续必然替换的方案。
- 不主动追求向后兼容：
  - 已废弃代码路径优先直接移除。
  - 仅在有明确用户依赖或迁移成本极高时保留兼容层。
  - 兼容代码必须有明确保留理由。
- 优先复用成熟可靠的库。

---

## Bug 分析与问题修复 🐛

- Bug 分析必须基于第一性原理：
  - 先复现问题。
  - 验证根因。
  - 不允许猜测式修改。
  - 不通过试错堆补丁掩盖真实问题。

- 定位最小根因，做最精准修复。

分析结果：

- 无问题：
  ```
  ✅OK
  ```

- 有问题：
  - 说明原因。
  - 提供验证依据。
  - 给出最小修复方案。

---

## Fallback 原则 🛡️

禁止：

- 添加无依据 fallback。
- 使用 fallback 隐藏主流程异常。
- 使用默认值掩盖数据错误。
- 使用异常捕获吞掉未知问题。

Fallback 仅用于：

- 明确设计的业务容错。
- 网络异常处理。
- 外部 API 边界处理。

Fallback 必须：

- 有明确触发条件。
- 有明确恢复策略。
- 不影响主流程错误暴露。
- 有日志或可观测性。

---

## Coding 规范 💻

- 中文回复，言简意赅，合理使用 Emoji。
- 仅实现必要功能，避免过度设计和顺手重构。
- 优先精准修复，严格控制修改范围。
- 代码检索优先使用 `codebase-memory-mcp`。
- 仅必要时使用 Read：
  - 禁止无目的读取完整文件。
  - 禁止批量加载无关上下文。
- 避免无必要测试，优先验证关键路径。

---

## Skill 使用 🧩

根据任务场景启用：

- **mattpocock-skills**：用于增强代码设计能力，包括类型建模、接口契约、复杂结构设计和可靠性提升。
- **superpowers**：用于复杂任务分析，包括问题拆解、思路推演、方案规划和执行管理。

无明确收益时不要调用。

---

## Plan Mode / Subagent 🤖

根据任务复杂度选择：

- 简单修改：直接执行。
- 多文件修改、架构调整、复杂分析：使用 Plan Mode 或 Subagent。

避免简单任务过度规划。

---

## 验证原则 ✅

验证优先级：

1. 静态检查。
2. 最小复现。
3. 关键路径验证。
4. 必要测试。

避免：

- 为简单修改运行完整测试套件。
- 为满足形式增加无价值测试。

---

## Git 提交规范 📦

采用 Conventional Commits，描述使用中文。

格式：

```text
<type>(<scope>): <中文描述>
```

- **type**：`feat` / `fix` / `docs` / `refactor` / `test` / `chore`
- **scope**：改动模块，可省略
- **描述**：中文，祈使句，不加句号

默认只写 subject 一行。非必要不加内容：

- 不加 body（仅当改动原因无法从 diff 看出时才加）
- 不加 `Co-Authored-By` 等 trailer
- 不加任何额外 commentary

---

## Agent skills

### Issue tracker

Issues 与 spec 以 markdown 文件形式存放在 `.scratch/<feature-slug>/`。见 `docs/agents/issue-tracker.md`。

### Triage labels

沿用五个规范角色的默认标签（needs-triage / needs-info / ready-for-agent / ready-for-human / wontfix）。见 `docs/agents/triage-labels.md`。

### Domain docs

Single-context：根目录 `CONTEXT.md` + `docs/adr/`。见 `docs/agents/domain.md`。
